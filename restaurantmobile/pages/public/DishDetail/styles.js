import { StyleSheet } from 'react-native';
import Colors from '@styles/colors';
import { editorialShadow } from '@styles/theme';

export default StyleSheet.create({
    heroWrap: { position: 'relative' },
    img: { width: '100%', height: 320, borderBottomLeftRadius: 32, borderBottomRightRadius: 32 },
    imagePlaceholder: { backgroundColor: Colors.surfaceContainerLow, justifyContent: 'center', alignItems: 'center' },
    headerActions: {
        position: 'absolute',
        top: 48,
        left: 16,
        right: 16,
        flexDirection: 'row',
        justifyContent: 'space-between',
        zIndex: 10,
    },
    actionBtn: {
        width: 40,
        height: 40,
        borderRadius: 20,
        backgroundColor: 'rgba(255,255,255,0.8)',
        justifyContent: 'center',
        alignItems: 'center',
        ...editorialShadow,
    },
    infoBox: {
        marginTop: -32,
        backgroundColor: Colors.surface,
        borderTopLeftRadius: 32,
        borderTopRightRadius: 32,
        paddingTop: 24,
        paddingHorizontal: 20,
        paddingBottom: 24,
    },
    titleRow: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'flex-start',
        gap: 16,
    },
    name: { fontSize: 26, fontWeight: '800', color: Colors.text, flex: 1, lineHeight: 32 },
    price: { fontSize: 24, color: Colors.primary, fontWeight: '800' },

    quickStatsRow: { flexDirection: 'row', alignItems: 'center', marginTop: 12, gap: 16 },
    ratingBadge: {
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: Colors.primaryLight,
        paddingHorizontal: 10,
        paddingVertical: 4,
        borderRadius: 8,
        gap: 6,
    },
    ratingBadgeText: { fontWeight: '700', color: Colors.primary, fontSize: 14 },
    ratingBadgeSub: { fontSize: 12, color: Colors.primary, fontWeight: '600' },
    prepTimeBadge: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 6,
    },
    prepTimeText: { fontSize: 14, fontWeight: '600', color: Colors.textSecondary },

    desc: { fontSize: 14, color: Colors.textSecondary, marginTop: 16, lineHeight: 22 },

    chatChefCard: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        backgroundColor: Colors.surfaceContainerLowest,
        padding: 14,
        borderRadius: 16,
        borderWidth: 1,
        borderColor: Colors.surfaceContainerLow,
        marginTop: 16,
    },
    chatChefInfo: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 12,
        flex: 1,
    },
    chatChefAvatar: {
        width: 48,
        height: 48,
        borderRadius: 24,
        backgroundColor: Colors.primaryLight,
        justifyContent: 'center',
        alignItems: 'center',
    },
    chatChefText: { flex: 1 },
    chatChefTitle: { fontSize: 12, color: Colors.textSecondary, fontWeight: '600', textTransform: 'uppercase', tracking: 0.5 },
    chatChefName: { fontSize: 16, color: Colors.text, fontWeight: '800', marginTop: 2 },
    chatChefAction: {
        backgroundColor: Colors.primary,
        width: 40,
        height: 40,
        borderRadius: 20,
        justifyContent: 'center',
        alignItems: 'center',
        elevation: 2,
        shadowColor: Colors.primary,
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.3,
        shadowRadius: 4,
    },

    sectionContainer: { marginTop: 20 },
    sectionTitle: { fontSize: 18, fontWeight: '800', color: Colors.text, marginBottom: 12 },

    ingredientGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
    ingredientChip: {
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: Colors.surfaceContainerLowest,
        borderColor: Colors.surfaceContainerLow,
        borderWidth: 1,
        paddingHorizontal: 12,
        paddingVertical: 6,
        borderRadius: 12,
        gap: 6,
    },
    ingredientText: { fontSize: 14, color: Colors.text, fontWeight: '500' },

    divider: { height: 1, backgroundColor: Colors.surfaceContainerLow, marginVertical: 20 },

    writeReviewCard: {
        backgroundColor: Colors.surfaceContainerLowest,
        padding: 16,
        borderRadius: 16,
        borderWidth: 1,
        borderColor: Colors.surfaceContainerLow,
    },
    starRow: { flexDirection: 'row', justifyContent: 'center', marginBottom: 16, gap: 8 },

    reviewItem: {
        padding: 16,
        backgroundColor: Colors.surfaceContainerLowest,
        borderRadius: 16,
        borderWidth: 1,
        borderColor: Colors.surfaceContainerLow,
        marginBottom: 12,
    },
    reviewTop: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 },
    reviewAuthorWrap: { flexDirection: 'row', alignItems: 'center', gap: 8 },
    reviewAvatar: { width: 32, height: 32, borderRadius: 16, backgroundColor: Colors.surfaceContainerLow, justifyContent: 'center', alignItems: 'center' },
    reviewAvatarText: { fontWeight: '800', fontSize: 12, color: Colors.textSecondary },
    reviewAuthor: { fontSize: 14, fontWeight: '700', color: Colors.text },
    reviewDate: { fontSize: 12, color: Colors.textSecondary },
    reviewStarBadge: {
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: Colors.star + '20',
        paddingHorizontal: 8,
        paddingVertical: 4,
        borderRadius: 6,
        gap: 4,
    },
    reviewStarText: { color: Colors.star, fontWeight: '800', fontSize: 12 },
    reviewComment: { color: Colors.textSecondary, fontSize: 14, lineHeight: 22 },

    emptyReview: { alignItems: 'center', paddingVertical: 20 },
    emptyReviewText: { fontSize: 14, color: Colors.textSecondary, marginTop: 10, textAlign: 'center' },

    bottomBar: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: 0,
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: Colors.surface,
        borderTopWidth: 1,
        borderTopColor: Colors.surfaceContainerLow,
        paddingHorizontal: 16,
        paddingTop: 16,
        paddingBottom: 24,
        gap: 12,
        elevation: 8,
        shadowColor: '#000',
        shadowOffset: { width: 0, height: -3 },
        shadowOpacity: 0.08,
        shadowRadius: 8,
    },
    stepper: {
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: Colors.surfaceContainerLow,
        borderRadius: 20,
        paddingHorizontal: 2,
        paddingVertical: 2,
    },
    stepBtn: { width: 38, height: 38, borderRadius: 14, backgroundColor: Colors.surfaceContainerLowest },
    quantityText: { minWidth: 36, textAlign: 'center', fontSize: 18, fontWeight: '800', color: Colors.text },
    cartBtn: {
        flex: 1,
        backgroundColor: Colors.primary,
        borderRadius: 20,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        paddingVertical: 14,
        paddingHorizontal: 8,
        gap: 4,
    },
    cartBtnText: {
        color: Colors.onPrimary,
        fontWeight: '700',
        fontSize: 14,
    },
    cartBtnDivider: {
        width: 4,
        height: 4,
        borderRadius: 2,
        backgroundColor: Colors.onPrimary + '80',
        marginHorizontal: 2,
    },
    cartBtnPrice: {
        color: Colors.onPrimary,
        fontWeight: '800',
        fontSize: 14,
    },
    centerState: {
        flex: 1,
        backgroundColor: Colors.surface,
        justifyContent: 'center',
        alignItems: 'center',
        paddingHorizontal: 32,
    },
    stateTitle: { fontSize: 22, fontWeight: '800', color: Colors.text, marginTop: 16 },
    stateText: { fontSize: 15, color: Colors.textSecondary, textAlign: 'center', marginTop: 10, lineHeight: 22 },
});
