$kind: http-request
description: UserViewSet does not register GET /api/users/{id}/; use list, current-user, chefs, or verify-chef routes instead.
url: "{{base_url}}/api/users/{{user_id}}/"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 7000
