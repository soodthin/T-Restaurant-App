$kind: http-request
url: "{{base_url}}/api/dishes/?menu_id={{menu_id}}"
method: GET
queryParams:
  menu_id: "{{menu_id}}"
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 4000
