$kind: http-request
url: "{{base_url}}/api/dishes/?my=true"
method: GET
queryParams:
  my: "true"
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 5000
