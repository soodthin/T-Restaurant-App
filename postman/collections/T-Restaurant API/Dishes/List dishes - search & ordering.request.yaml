$kind: http-request
url: "{{base_url}}/api/dishes/?search=bun&ordering=-avg_rating"
method: GET
queryParams:
  search: bun
  ordering: -avg_rating
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
